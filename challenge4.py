# Inputs a list.
# Outputs a sum of the elements in the list.
# Easy option using sum function, it takes an iterable as
# its imnput and returns the sume of the elements.

number_list = [1, 2, 3, 4, 5, 6, 20, 88]
print(sum(number_list))
